---
phase: 260429-u61
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/index.html
  - static/index.html
  - frontend/src/hud.js
  - frontend/src/tray.js
  - frontend/src/selection.js
  - frontend/src/completion.js
autonomous: false
requirements:
  - U61-01
  - U61-02
  - U61-03

must_haves:
  truths:
    - "Browser tab title reads 'Echo's Big Birthday Adventure!' on every page"
    - "The selection screen brand wordmark reads 'Echo's Big Birthday Adventure!' (or a clean compact equivalent) instead of 'Brick Basket'"
    - "Selection screen, build screen, completion screen, and celebration screen each show at least one 🍊 or 🍎 emoji in user-visible UI text"
    - "Set picker still loads the catalogue from /api/sets and at least one set card is clickable into a build"
    - "A build session can still be entered, a piece can be picked from the tray, and a brick can be placed on the grid (no functional regression)"
    - "The completion screen still displays elapsed time and the Build Again / New Set buttons still work"
    - "The all-complete celebration screen still renders confetti emojis (existing fruit confetti unaffected)"
  artifacts:
    - path: "frontend/index.html"
      provides: "Source HTML — title tag + brand wordmark + screen headings"
      contains: "Echo's Big Birthday Adventure"
    - path: "static/index.html"
      provides: "Deployed HTML (Vite build output) — same edits as frontend/index.html"
      contains: "Echo's Big Birthday Adventure"
    - path: "frontend/src/hud.js"
      provides: "Mentor tips + 'Goal Pieces' / 'Instructions' labels with emoji sprinkle"
    - path: "frontend/src/tray.js"
      provides: "'Select a piece' empty-state text with emoji"
    - path: "frontend/src/selection.js"
      provides: "BUILD / REBUILD / CONTINUE button labels + streak pill text with emoji"
    - path: "frontend/src/completion.js"
      provides: "Untouched logic, but verified that FRUITS confetti array still contains 🍊 and 🍎"
  key_links:
    - from: "<title>"
      to: "browser tab"
      via: "HTML <head>"
      pattern: "Echo's Big Birthday Adventure"
    - from: ".brand-wordmark"
      to: "selection-header logo"
      via: "DOM"
      pattern: "Echo"
    - from: "MENTOR_TIPS"
      to: ".hud-mentor-tip"
      via: "renderHUD"
      pattern: "🍊|🍎"
---

<objective>
Rebrand the app to "Echo's Big Birthday Adventure!" everywhere user-visible, and sprinkle 🍊 / 🍎 (with occasional 🍏) emojis generously through the DOM-rendered UI text. Pure presentational change — zero logic, zero data-structure edits, zero Three.js scene-text changes.

Purpose: The user wants a celebratory birthday-themed coat of paint over the existing fruit-stand polish, without touching any of the working 3D builder, set catalogue, or interaction logic.
Output: Updated HTML titles + brand text + button labels + headings + mentor tips, with emoji garnish in DOM text only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

<!-- Source-of-truth files. frontend/index.html is the Vite source; static/index.html is the prebuilt deploy artifact. Both must be updated in lockstep so the change is visible in both `npm run dev` and direct Flask serving. -->

@frontend/index.html
@static/index.html
@frontend/src/hud.js
@frontend/src/tray.js
@frontend/src/selection.js
@frontend/src/completion.js

<interfaces>
<!-- Key text-rendering points the executor must touch. Extracted from grep so no exploration needed. -->

frontend/index.html / static/index.html — IDENTICAL EDITS REQUIRED IN BOTH:
  - <title>Brick Basket</title>                                  → <title>🍊 Echo's Big Birthday Adventure! 🍎</title>
  - <span class="brand-wordmark">Brick <span class="brand-wordmark-italic">Basket</span></span>
                                                                  → Echo's Big Birthday <span class="brand-wordmark-italic">Adventure!</span>  (with leading 🍊 and trailing 🍎 outside the span if they fit)
  - <h1>Choose Your Next Build</h1>                              → emoji-prefixed heading
  - <p>Select a project and start assembling your masterpiece.</p> → emoji-sprinkled copy
  - <button class="category-tab" data-filter="fruits">Fruits</button> → "🍊 Fruits" (data-filter UNCHANGED — JS keys on it)
  - <button class="category-tab" data-filter="bosses">Bosses</button> → "🍎 Bosses"
  - <div class="boss-collapsed-title">Boss Challenges</div>      → emoji-prefixed
  - <button id="quit-btn" class="keycap">Save &amp; Quit</button> → "🍎 Save & Quit" or similar (LABEL ONLY — id stays #quit-btn)
  - <div class="active-piece-label">Active Piece</div>           → "🍊 Active Piece"
  - <h1 class="completion-title">Build Complete!</h1>            → "🍊 Build Complete! 🍎"
  - <button class="completion-btn" id="build-again-btn">Build Again</button>   → "🍎 Build Again"   (id UNCHANGED)
  - <button id="new-set-btn">New Set</button>                    → "🍊 New Set"        (id UNCHANGED)
  - <h1 class="celebration-title">Master Basket</h1>             → "Echo, You're a Legend!" or similar with emojis
  - <div class="celebration-wish-label">You earned 5 wishes!</div> → "🍊 You earned 5 wishes! 🍎"
  - <div class="boss-collapsed-sub">4 builds. Tap to view.</div> → emoji garnish OK
  - <div class="boss-modal-title">Boss Challenges</div>          → emoji-prefixed

frontend/src/hud.js:
  Line 3-9: MENTOR_TIPS array — prepend each tip with 🍊 or 🍎 alternating, e.g.:
    '🍊 Tap R to rotate the held piece.',
    '🍎 Scroll to zoom — drag to orbit.',
    ...
  Line 62: title.textContent = 'Goal Pieces';        → '🍊 Goal Pieces'
  Line 140: instrLabel.textContent = 'Instructions'; → '🍎 Instructions'
  Line 163: avatar.textContent = 'O';                 → leave as 'O' (it's the avatar initial, not a mentor name)
  DO NOT touch line 96 (`Step ${i}`), line 110 (✓), line 53 step meta, line 175 step counter — those are functional.

frontend/src/tray.js:
  Line 257: name.textContent = 'Select a piece';     → '🍊 Select a piece 🍎'
  Line 206: desc.textContent = `${_colorName(piece.color)} · GLOSS`; → leave alone (data-driven)
  DO NOT touch _formatType, _colorName, or the tray data flow.

frontend/src/selection.js:
  Line 164 / 444: streakEl.textContent = `${_readStreak()}-day streak`; → `🍊 ${_readStreak()}-day streak` (or 🍎 — pick one)
  Line 401: btn.textContent = 'CONTINUE'; → '🍊 CONTINUE'
  Line 404: btn.textContent = 'REBUILD';  → '🍏 REBUILD'
  Line 406: btn.textContent = 'BUILD';    → '🍎 BUILD'
  DO NOT touch line 367 (set name), line 375 (stars), line 357 (✓ badge — functional check icon), line 349 (pct%), line 379-390 (difficulty/best meta), line 205 error string is fine to garnish but optional.
  DO NOT touch the `brickbuilder_streak` localStorage key (line 20) — that's a storage key, not UI text.

frontend/src/completion.js:
  Line 153: const FRUITS = ['🍊', '🍎', '🍑', '🍍', '🌿', '🧃'];
  Already contains 🍊 and 🍎 — verify only, no edit needed. If desired, add another 🍏 to bias toward the birthday palette. Optional.

DO NOT TOUCH:
  - sets/*.json (brick IDs, color hex, types)
  - app.py (server logic — the page is served as a static file, no template title)
  - frontend/src/geometry.js, scene.js, grid.js, ghost.js, interaction.js, cursor.js, state.js, stressTest.js (pure logic / 3D)
  - tests/*
  - data-filter attribute values, button id values, CSS class names, localStorage keys
  - Three.js scene labels (none exist as text — leave it that way)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebrand HTML titles, headings, and button labels with emoji sprinkle</name>
  <files>frontend/index.html, static/index.html</files>
  <action>
Apply the SAME set of edits to both `frontend/index.html` (Vite source) and `static/index.html` (built artifact currently served by Flask). Use the Read tool first on each file, then use Edit.

Required text replacements (apply to both files):

1. `<title>Brick Basket</title>` → `<title>🍊 Echo's Big Birthday Adventure! 🍎</title>`

2. Brand wordmark in `.selection-logo` (around line 1194 / 1352):
   FROM: `<span class="brand-wordmark">Brick <span class="brand-wordmark-italic">Basket</span></span>`
   TO:   `<span class="brand-wordmark">🍊 Echo's Big Birthday <span class="brand-wordmark-italic">Adventure!</span> 🍎</span>`

3. Selection title area:
   - `<h1>Choose Your Next Build</h1>` → `<h1>🍊 Choose Your Next Build 🍎</h1>`
   - `<p>Select a project and start assembling your masterpiece.</p>` → `<p>🍎 It's Echo's birthday — pick a build and start celebrating! 🍊</p>`

4. Category tabs (CRITICAL: only edit the visible label between `>...</button>`. The `data-filter="..."` attribute MUST stay byte-for-byte identical — selection.js keys on it):
   - `>All</button>` → `>🍊 All</button>`
   - `>Fruits</button>` → `>🍊 Fruits</button>`
   - `>Snacks</button>` → `>🍎 Snacks</button>`
   - `>Bosses</button>` → `>🍎 Bosses</button>`

5. Boss collapsed card + modal:
   - `<div class="boss-collapsed-title">Boss Challenges</div>` → `<div class="boss-collapsed-title">🍎 Boss Challenges 🍊</div>`
   - `<div class="boss-collapsed-sub">4 builds. Tap to view.</div>` → `<div class="boss-collapsed-sub">🍊 4 builds. Tap to view.</div>`
   - `<div class="boss-modal-title">Boss Challenges</div>` → `<div class="boss-modal-title">🍎 Boss Challenges 🍊</div>`
   - `<div class="boss-collapsed-sub">Think you're ready? These builds will test your patience.</div>` → `<div class="boss-collapsed-sub">🍏 Think you're ready? These builds will test your patience.</div>`

6. Reset progress button: `<button id="reset-progress-btn">Reset Progress</button>` → `<button id="reset-progress-btn">Reset Progress 🍊</button>` (id MUST stay)

7. Build UI top bar:
   - `<button id="quit-btn" class="keycap">Save &amp; Quit</button>` → `<button id="quit-btn" class="keycap">🍊 Save &amp; Quit</button>` (id stays)

8. Tray active-piece card:
   - `<div class="active-piece-label">Active Piece</div>` → `<div class="active-piece-label">🍊 Active Piece</div>`

9. Completion screen:
   - `<h1 class="completion-title">Build Complete!</h1>` → `<h1 class="completion-title">🍊 Build Complete! 🍎</h1>`
   - `<button class="completion-btn" id="build-again-btn">Build Again</button>` → `<button class="completion-btn" id="build-again-btn">🍎 Build Again</button>`
   - `<button class="completion-btn completion-btn-secondary" id="new-set-btn">New Set</button>` → `<button class="completion-btn completion-btn-secondary" id="new-set-btn">🍊 New Set</button>`

10. Celebration screen:
    - Trophy `<div class="celebration-trophy">🍊</div>` — already an emoji, leave as-is.
    - `<h1 class="celebration-title">Master Basket</h1>` → `<h1 class="celebration-title">Echo's Big Birthday Adventure!</h1>` (the gradient text fill clips; emojis don't render under -webkit-text-fill-color: transparent — keep emojis OUT of this specific h1 to avoid invisible glyphs. Add emoji garnish to subtitle below instead.)
    - `<p class="celebration-subtitle">Every set. Every brick. Every step.<br>You completed them all.</p>` → `<p class="celebration-subtitle">🍊 Every set. Every brick. Every step. 🍎<br>Echo, you completed them all!</p>`
    - `<div class="celebration-wish-label">You earned 5 wishes!</div>` → `<div class="celebration-wish-label">🍊 You earned 5 wishes! 🍎</div>`
    - `<div class="celebration-wish-hint">Screenshot as evidence.</div>` → `<div class="celebration-wish-hint">🍏 Screenshot as evidence.</div>`
    - `<button class="celebration-btn" id="celebration-close-btn">Back to Sets</button>` → `<button class="celebration-btn" id="celebration-close-btn">🍊 Back to Sets</button>`

CRITICAL CONSTRAINTS — DO NOT MODIFY:
- Any element `id` attribute
- Any `data-filter`, `data-state`, `class`, or other attribute
- Any CSS rule (the `<style>` block stays untouched)
- The `<script>` tag at the bottom of static/index.html (`/assets/index-BdO6o4wk.js`)
- The `<script type="module" src="/src/main.js"></script>` tag in frontend/index.html
- Any HTML structure — only the text content between tags changes

WHY: selection.js queries on `data-filter`, button ids drive event handlers, and the bundled JS in `static/assets/index-*.js` was built against these exact ids/attrs.

Apply each edit identically to both files. After editing, the only differences between them should be the same differences that existed before (the script tags, formatting whitespace).
  </action>
  <verify>
    <automated>grep -c "Echo's Big Birthday" frontend/index.html static/index.html && grep -c "🍊\|🍎" frontend/index.html static/index.html && ! grep -n 'Brick Basket' frontend/index.html static/index.html && ! grep -n 'Master Basket' frontend/index.html static/index.html && grep -E 'data-filter="(all|fruits|snacks|bosses)"' frontend/index.html static/index.html | wc -l | grep -q 8 && grep -c 'id="quit-btn"\|id="build-again-btn"\|id="new-set-btn"\|id="reset-progress-btn"\|id="celebration-close-btn"' frontend/index.html static/index.html</automated>
  </verify>
  <done>
- Both HTML files contain "Echo's Big Birthday Adventure!" in title and brand
- Multiple 🍊/🍎 occurrences across selection, build, completion, celebration screens
- "Brick Basket" and "Master Basket" no longer appear anywhere
- All four `data-filter="..."` values intact (8 total, 4 per file)
- All five tracked button ids still present in both files
- No CSS or script tag changes
  </done>
</task>

<task type="auto">
  <name>Task 2: Sprinkle emojis into JS-rendered text (mentor tips, labels, button strings, streak pill)</name>
  <files>frontend/src/hud.js, frontend/src/tray.js, frontend/src/selection.js</files>
  <action>
Make targeted, surgical edits to JS string literals only. Do NOT touch any function logic, variable names, parameter handling, or control flow.

**frontend/src/hud.js:**

1. Replace the MENTOR_TIPS array (lines 3-9) so each tip is prefixed with an alternating 🍊 / 🍎 / 🍏 fruit emoji and a birthday flavour where natural:
```javascript
const MENTOR_TIPS = [
  '🍊 Tap R to rotate the held piece.',
  '🍎 Scroll to zoom — drag to orbit.',
  '🍏 Click the glowing ghost to lock a brick into place.',
  '🍊 Right-drag pans the camera.',
  '🍎 Each step lights up only the bricks you need next — Echo would be proud.',
];
```

2. Line 62: change `title.textContent = 'Goal Pieces';` → `title.textContent = '🍊 Goal Pieces';`

3. Line 140: change `instrLabel.textContent = 'Instructions';` → `instrLabel.textContent = '🍎 Instructions';`

DO NOT TOUCH:
- Line 50 (`LVL ${stepNumber}` — functional)
- Line 53 (step meta — functional)
- Line 96 (`Step ${i}` — functional)
- Line 110 (✓ checkmark — functional)
- Line 115 (`${i}/${totalSteps}` — functional)
- Line 142 (`Step ${stepNumber} of ${totalSteps}` — functional)
- Line 149 (`${step.description}` — DATA from set JSON, must NOT be wrapped or modified)
- Line 163 (avatar 'O' initial — visual design)
- Line 175 (top step text — functional)
- Line 184 (setName — DATA)

**frontend/src/tray.js:**

1. Line 257: change `if (name) name.textContent = 'Select a piece';` → `if (name) name.textContent = '🍊 Select a piece 🍎';`

DO NOT TOUCH any other line — `_formatType`, `_colorName`, the tray render loop, image attachment, swatch logic, footprint label all stay byte-identical.

**frontend/src/selection.js:**

1. Lines 164 AND 444 (two identical occurrences — find both):
   `if (streakEl) streakEl.textContent = `${_readStreak()}-day streak`;`
   becomes:
   `if (streakEl) streakEl.textContent = `🍊 ${_readStreak()}-day streak`;`

2. Line 401: `btn.textContent = 'CONTINUE';` → `btn.textContent = '🍊 CONTINUE';`

3. Line 404: `btn.textContent = 'REBUILD';` → `btn.textContent = '🍏 REBUILD';`

4. Line 406: `btn.textContent = 'BUILD';` → `btn.textContent = '🍎 BUILD';`

DO NOT TOUCH:
- Line 20 (`localStorage.getItem('brickbuilder_streak')` — STORAGE KEY, changing it would wipe the user's streak progress)
- Line 367 (`name.textContent = setMeta.name;` — DATA from API)
- Line 349 (progressBadge percentage — functional)
- Line 357 (`badge.textContent = '✓';` — completion checkmark icon, leave as-is)
- Line 375 (stars rendering — functional)
- Line 379, 390 (difficulty / best meta — functional)
- Line 205 (error message — optional to garnish but skip for now)
- Any function definition, any setMeta access, any DOM lookup, any event listener wiring
- The localStorage key string `'brickbuilder_streak'` ANYWHERE in the file

The point: only the FOUR/FIVE specific string literals listed above change. Nothing else.

NOTE on completion.js: do NOT modify. Its `FRUITS` array on line 153 already includes 🍊 and 🍎 — that satisfies the celebration-confetti emoji requirement without any edit. Leaving it untouched also de-risks the confetti animation.
  </action>
  <verify>
    <automated>grep -q "🍊 Tap R" frontend/src/hud.js && grep -q "🍊 Goal Pieces" frontend/src/hud.js && grep -q "🍎 Instructions" frontend/src/hud.js && grep -q "🍊 Select a piece" frontend/src/tray.js && grep -q "🍊 CONTINUE" frontend/src/selection.js && grep -q "🍏 REBUILD" frontend/src/selection.js && grep -q "🍎 BUILD" frontend/src/selection.js && grep -q "🍊 \${_readStreak" frontend/src/selection.js && grep -q "brickbuilder_streak" frontend/src/selection.js && node --check frontend/src/hud.js && node --check frontend/src/tray.js && node --check frontend/src/selection.js</automated>
  </verify>
  <done>
- MENTOR_TIPS prefixed with fruit emojis (5 tips, all start with an emoji)
- Goal Pieces / Instructions labels prefixed
- Tray "Select a piece" wrapped with emojis
- CONTINUE / REBUILD / BUILD button labels prefixed
- Streak pill text prefixed with 🍊 (both occurrences)
- localStorage key `'brickbuilder_streak'` STILL PRESENT (not renamed)
- All three JS files pass `node --check` (syntax valid)
- No other lines in these files modified
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manually verify the rebrand visually + confirm no functional regression</name>
  <what-built>
- HTML rebrand: page title, brand wordmark, all screen headings, all button labels updated to "Echo's Big Birthday Adventure!" theme with 🍊/🍎/🍏 emoji sprinkle in both `frontend/index.html` and `static/index.html`.
- JS string sprinkle: mentor tips, HUD labels, tray empty-state, selection-screen button labels, and streak pill all carry fruit emojis.
- All ids, data-attrs, classes, localStorage keys, and CSS rules untouched.
  </what-built>
  <how-to-verify>
Run the app via Flask (which serves the prebuilt `static/index.html`):

```bash
# Terminal 1
cd /Users/chien/lego-builder
source .venv/bin/activate 2>/dev/null || python3 -m venv .venv && source .venv/bin/activate
pip install -q -r requirements.txt
flask --app app run --port 5000
```

Open http://localhost:5000 in a browser and confirm:

1. **Browser tab title** reads "🍊 Echo's Big Birthday Adventure! 🍎"
2. **Selection screen header** brand reads "🍊 Echo's Big Birthday Adventure! 🍎" (or your chosen variant)
3. **"Choose Your Next Build"** heading shows fruit emojis
4. **Category tabs** (All / Fruits / Snacks / Bosses) each have a fruit emoji prefix; clicking each tab still filters the catalogue (FUNCTIONAL CHECK)
5. **Set cards** still load — at least one card visible with thumbnail, name, BUILD button reading "🍎 BUILD"
6. **Streak pill** in the top-right reads "🍊 0-day streak" (or whatever the user's streak is)
7. Click any set card → enters build mode:
   - **Top bar** "Save & Quit" button reads "🍊 Save & Quit"
   - **Tray panel** "🍊 Active Piece" label visible
   - **Tray empty state** (before selecting): "🍊 Select a piece 🍎"
   - **HUD right panel** "🍊 Goal Pieces" and "🍎 Instructions" labels visible
   - **Mentor tip row** at bottom of HUD shows a tip starting with 🍊, 🍎, or 🍏
   - **Pick a piece from the tray** → name updates correctly (FUNCTIONAL CHECK — no regression)
   - **Click the ghost on the grid** → brick snaps into place (FUNCTIONAL CHECK — placement still works)
8. Complete a build (or fast-forward to completion screen via dev tooling if known):
   - **Completion title** "🍊 Build Complete! 🍎"
   - **Build Again** button reads "🍎 Build Again"
   - **New Set** button reads "🍊 New Set"
   - Click Build Again → returns to build screen (FUNCTIONAL CHECK)
9. **All-complete celebration** (if reachable): subtitle has 🍊/🍎, wish label has 🍊/🍎, button reads "🍊 Back to Sets"
10. **Hard refresh + check streak persistence**: streak number from before should still be there (confirms `brickbuilder_streak` localStorage key wasn't renamed)
11. **Reset Progress button** still works (FUNCTIONAL CHECK)
12. **Run dev mode too** to confirm Vite source is in sync:
    ```bash
    cd frontend && npm install --silent && npm run dev
    ```
    Visit http://localhost:5173 — should look identical to the Flask-served version.

If any of the FUNCTIONAL CHECKS fail, or if any "Brick Basket" / "Master Basket" string is still visible, report back the issue.

If everything passes visually and functionally, type "approved" to continue.
  </how-to-verify>
  <resume-signal>Type "approved" to commit the rebrand, or describe any issues spotted.</resume-signal>
</task>

</tasks>

<verification>
- `grep -r "Brick Basket\|Master Basket" frontend/ static/ frontend/src/` returns zero matches in user-visible text (CSS/JS comments are acceptable; check is for displayed strings)
- `grep -c "🍊\|🍎\|🍏" frontend/index.html static/index.html` shows multiple matches in each file
- `node --check` passes for hud.js, tray.js, selection.js
- `python3 -c "import app"` (or running `flask --app app run`) starts without error — confirms Python side untouched
- Manual: app loads in browser, tab title is updated, selection screen brand is updated, build flow still works end-to-end (pick → place → complete)
- Manual: streak persists across reload (storage key intact)
</verification>

<success_criteria>
- App is rebranded to "Echo's Big Birthday Adventure!" everywhere user-visible (browser tab, header, completion, celebration)
- 🍊 and 🍎 emojis appear generously across selection, build, completion, and celebration UIs
- Set picker, brick placement, completion flow, and celebration screen all still function with zero regression
- Streak counter, set IDs, brick IDs, color hex codes, and all data structures untouched
- Both `frontend/index.html` (Vite source) and `static/index.html` (Flask-served) are in lockstep
</success_criteria>

<output>
After completion, create `.planning/quick/260429-u61-add-orange-and-apple-emojis-throughout-u/260429-u61-SUMMARY.md` describing the rebrand with a list of edited files and a screenshot/note of what the user verified.
</output>
